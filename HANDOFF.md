# 灵爪收纳开发交接与复盘

更新时间：2026-08-21

## 这份文档是什么

这是一份面向后续开发者/代理的 `HANDOFF.md`。它把当前工作目录中能找到的灵爪收纳开发材料、实现状态、验证证据、历史判断和遗留问题集中到一起。

重要边界：当前工作区没有保存完整的 ChatGPT/Codex 历史聊天导出，也没有可用的灵爪 Git 提交历史。因此“所有聊天记录”无法逐条复原。本文件基于当前对话、项目文件、开发分析文档、实验记录、测试代码、截图、模型资源和提交包说明进行还原；没有证据的内容不写成已完成事实。

## 一句话结论

灵爪收纳已经从“SwiftUI 页面入口 + DemoData”推进到“真实图片/视频帧识别 + 用户确认 + 本地规划 + 分步执行 + raster mask 引导 + 成果沉淀”的可演示原型。

当前最强的成果是产品闭环和本地实例分割链路；当前最主要的风险是：识别模型仍是通用 COCO 模型、细小物体和高杂乱场景召回不稳定、真实 ARKit/RoomPlan 仍缺真机证据，以及最终提交包在 2026-08-21 的当前环境构建时出现 Core ML 模型编译失败。

## 项目位置与版本关系

### 主要开发工作目录

本文件所在目录：

`/Users/qszjlzz/Desktop/移动应用比赛/灵爪收纳`

目录包含：

- `App/`：应用入口、依赖注入、状态和数据生命周期。
- `Features/`：空间、采集、分类库、规划、执行、社区和个人中心。
- `Models/`：识别结果和业务模型。
- `Services/`：Vision、Core ML、YOLO、规划、存储和凭据服务。
- `Resources/`：Core ML 模型、真实评测照片、视频帧和界面资源。
- `SmartPawTests/`：单元测试和识别评测。
- `app-analysis/`：截图、Figma 参考图、模型实验产物。
- `video-analysis/`：参考视频抽帧和 contact sheet。
- `docs/`：开发过程中的分析、基线和验收记录。
- `logs/`：当前目录下没有可用于还原完整聊天内容的文本聊天日志。

### 同级交付物

工作区还存在多个历史/交付副本：

- `../灵爪收纳-移动应用创新赛源代码/`
- `../灵爪收纳-移动应用创新赛源代码-备份/`
- `../灵爪收纳-移动应用创新赛提交包/`
- `../灵爪收纳-移动应用创新赛提交包-最终/`

其中 `灵爪收纳-移动应用创新赛提交包-最终/` 的源码文件更新时间到 2026-08-08，是目前看到的最新提交包候选；但本复盘的开发过程主线以本目录的 `docs/` 和实验材料为准。不要把不同副本的测试数字和代码状态混在一起。

本目录本身不是 Git 仓库，无法用 commit 顺序补齐历史。文件更新时间和文档中的日期是目前可用的时间线证据。

## 我实际查看过什么

### 过程文档

- `docs/灵爪收纳-视频与App现状分析.md`
- `docs/灵爪收纳-视频页面状态表.md`
- `docs/灵爪收纳-继续开发方向.md`
- `docs/灵爪收纳-功能完成与验收状态.md`
- `docs/灵爪收纳-参考视频严格视觉对照.md`
- `docs/灵爪收纳-真实帧识别基线.md`
- `docs/识别引擎v2状态.md`
- `docs/识别实验-逐图证据-2026-08-03.md`
- `docs/识别实验-逐图证据-2026-08-04.md`
- `docs/对象级识别基线-2026-08-04.md`
- `docs/Figma前端界面与产品逻辑.md`
- `docs/figma-screen-map.md`

### 代码和工程材料

- `SmartPaw.xcodeproj` 和 `SmartPawTests/SmartPawTests.swift`。
- `App/`、`Features/`、`Models/`、`Services/` 的 Swift 实现。
- `Resources/Models/` 中的 DeepLab、YOLO 和 MobileCLIP 资源。
- `Resources/ExternalScanTests/` 和 `Resources/EvaluationScanTests/` 中的真实图片、标注和测试清单。
- `app-analysis/screenshots/` 中的运行截图。
- `video-analysis/frames/` 和 `video-analysis/contact-sheets/` 中的参考视频抽帧。
- 根目录的参考视频 `e4f91991caef61ce0e5cfb43f740c2a6.mp4`、汇报 PPT 和说明文档。

### 当前对话能确认的内容

本次对话中确认了：用户要求后续只看灵爪相关目录；当前希望将前面的开发过程、对话能反映出的优缺点和已查看材料沉淀为 `HANDOFF.md`。除此之外，本地没有完整聊天记录文件可供逐条引用。

## 开发演进

### 阶段一：从界面和 DemoData 起步

早期 App 已有多个 SwiftUI 入口，但产品链路比较分散，首页、采集、规划、执行、社区和个人页更像功能集合。早期分析明确指出：页面能展示，但无法证明真实识别、AR 和真实收纳流程。

主要问题：

- 过度依赖 `DemoRoomPreview`、`DemoData` 和占位式前后对比。
- 用假文字图或演示图测试，不能证明视觉识别能力。
- 页面有入口，但缺少“看到问题 -> 采集 -> 识别确认 -> 生成方案 -> 执行 -> 完成沉淀”的强引导。
- 首页偏通用卡片堆叠，缺少视频中“空间状态 + 下一步动作”的产品主场。

### 阶段二：参考视频拆解和真实素材建立

项目抽取参考视频约 57 张帧，按约 2 秒间隔生成 contact sheet，并把视频拆成更细的产品状态：品牌/痛点、多维采集、识别确认、需求输入、区域选择、方案生成、分步执行、完成照、前后对比、社区复刻和个人成就。

这一阶段形成的正确方向是：先把视频表达转成页面状态表，再用真实素材验证每个状态，而不是继续堆页面。

### 阶段三：真实帧识别和可确认结果

识别链路从 Apple Vision、OCR、前景区域和 DeepLab 兜底开始，逐步改成真实帧优先。识别失败时不再直接中断，而是生成“待确认区域”，进入用户确认流程；识别结果可以携带归一化 `arHint` 和 `arMask`。

这一步建立了重要产品原则：低置信度可以让用户确认或修正，但不能用固定假框制造“识别成功”。

### 阶段四：YOLO 实例分割接入

随后接入 YOLO11n-seg Core ML 模型：

- 1280 模型优先，640 模型回退。
- 使用官方 letterbox，避免输入拉伸导致坐标错位。
- 解析检测张量和 mask proto，执行置信度筛选、同类别 NMS、mask 合成和原图坐标回映射。
- YOLO 无结果时再回退 DeepLabV3，最后回退 Vision/OCR/显著区域管线。
- UI 统一接收 raster mask，不把矩形框或手工 polygon 当成真实 mask。
- 实时 AR 路径增加模型缓存，避免每帧重复加载模型。

复核过程中修复过输入拉伸、mask 坐标映射、外层 frame 二次居中、跨类别错误抑制和小物体过滤等问题。这些修复比单纯调阈值更有价值，因为它们解决的是管线正确性。

### 阶段五：从识别能力推进到产品闭环

当前代码和验收文档显示，以下流程已经实现：

`拍摄/导入 -> 识别 -> 用户确认和修正 -> 目标/区域/风格/时间输入 -> 本地规则方案 -> 区域选择 -> 分步执行 -> mask 引导 -> 完成照 -> 前后对比/成就/社区沉淀`

已实现模块包括：

- 采集：相机优先、相册导入、RoomPlan 真机入口、模拟器真实照片演练、识别失败补救。
- 识别确认：名称、分类、归位区修正；低置信度候选可人工确认。
- 规划：本地规则引擎，支持目标、区域、标签、参考图、风格和时间预算；可选兼容 Responses API 的云端增强。
- 执行：工具清单、区域选择、步骤状态、mask 引导、完成照和前后对比。
- 分类库：搜索、新增、详情、来源空间、位置建议、编辑和删除确认。
- 社区：案例详情、点赞、收藏、系统分享、复刻确认和完成案例发布。
- 我的：等级、徽章、完成时间线、日程增删改完和本地通知调度。
- 数据和凭据：JSON 主文件/备份恢复、旧数据迁移、Keychain 保存 API Key、数据重置。

## 当前有效的验证结论

### 产品和代码完成度

`docs/灵爪收纳-功能完成与验收状态.md`（2026-07-27）记录：构建通过、33 个 XCTest 通过、静态分析通过，独立照片连续推理通过；两个独立复核曾检查并修复并发扫描错配、RoomPlan 结果丢失、通知竞态、明文密钥、备份恢复和伪 mask 混入等问题。

这些数字属于该文档对应版本，不能自动代表 2026-08-08 提交包或 2026-08-21 当前环境。

### 识别能力

2026-08-04 的对象级基线是目前最有价值的量化证据：

- 6 张真实图、16 个明确标注目标。
- 目标召回 16/16，记录为 100%。
- 平均框 IoU 0.630，通过 0.620 门禁。
- 完整 10 图场景集保持场景命中，但这不是完整 precision、F1、AP 或 mask IoU/Dice 数据集。

正确解释是：在这份有限、明确标注的评测清单上通过了对象匹配门禁；不能外推成“通用识别准确率 100%”。

已知弱项仍然包括细线、线缆、书页边界、平板、收纳盒、垃圾堆和高杂乱房间。YOLO11n-seg 是通用 COCO 模型，不是收纳场景专用模型。

### 视觉对照

当前 App 已达到与参考视频相同的大方向：相机/照片主画面、扫描控制区、实例 raster mask、停止/完成操作。但严格对照仍是“部分一致”，不是像素级或动效级复刻：

- 大物体如电脑、杯子、椅子和书堆可以看到较合理的 mask。
- 线缆、笔尖和书页等细边仍受 320 x 320 proto 限制。
- 模拟器使用静态真实照片，不能证明真机移动时的 mask 稳定、延迟、漂移和发热。
- EdgeSAM/MobileSAM 尚未被可靠接入，下载失败或未验证的模型不能算已完成。

### 当前构建验证

2026-08-21 对最新提交包候选执行了：

```bash
xcodebuild -project "灵爪收纳/SmartPaw.xcodeproj" \
  -scheme SmartPaw \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/smartpaw-derived \
  build CODE_SIGNING_ALLOWED=NO
```

本次结果是 `BUILD FAILED`。失败集中在 Core ML 模型编译任务，包括：

- `yolov8s-worldv2.mlpackage`
- `yoloe-context-seg.mlpackage`
- `yoloe-11s-seg.mlpackage`
- `yolo11n-seg.mlpackage`
- `yolo11n-seg-640.mlpackage`

同时机器上的 CoreSimulator 服务无法发现模拟器运行时。这个结果与旧文档中“构建通过”的历史结果冲突，必须以重新清理环境并在当前最新提交包上复测后的结果为准，不能直接宣称当前包可构建运行。

## 做得好的地方

### 1. 从“看起来有功能”转向“用证据证明能力”

后续开发明确放弃用假文字图证明识别能力，把真实视频帧、独立真实照片、标注框、截图和测试结果纳入基线。这是整个项目最重要的质量提升。

### 2. 识别链路有明确的降级策略

YOLO -> DeepLab -> Vision/OCR/显著区域的级联，让 App 在模型没有有效候选时仍能进入“待确认”流程；同时通过 raster mask 类型约束，避免 UI 把伪造的矩形或 polygon 当成真实分割。

### 3. 发现了真正的坐标和后处理问题

letterbox、padding 回映射、同类别 NMS、跨层 mask 合并和照片显示矩形对齐，都是会直接造成“识别看似成功但画面错位”的工程问题。通过 Python Core ML 与 Swift 输出对照来修正，比盲调 UI 更可靠。

### 4. 产品闭环比单点 AI 更完整

识别结果可以进入确认、规划、执行、完成照、前后对比、徽章和社区复刻，AI 不是孤立 Demo。没有云端 Key 时仍能走本地规则引擎，保证了演示可用性。

### 5. 对结论边界比较诚实

文档主动区分场景命中、对象召回、框 IoU、mask 质量和真机 AR 能力，没有把有限数据写成普遍准确率，也没有把未下载的 SAM 模型写成已接入。

## 做得不够好的地方

### 1. 版本和证据没有统一收口

多个副本并存，文档日期从 7 月 22 日到 8 月 4 日，最新提交包又到 8 月 8 日；历史文档中存在“18 个测试”“20 个测试”“33 个测试”“39 个测试”等不同数字。它们可能对应不同版本，但没有统一的版本标识、构建号和结果目录，导致交接时容易误读。

改进：以后每次验收都记录源码目录、提交包路径、日期、Xcode 版本、目标设备、命令、结果包路径和测试数量。

### 2. 过早把功能完成写成了验收完成

7 月底文档称功能和自动验收已完成，但后续 8 月仍在继续识别模型、目标级评测和阈值实验；8 月 21 日最新提交包在当前环境又出现模型编译失败。功能闭环完成不等于交付包可重建，也不等于真机 AR 已验收。

改进：将“功能实现”“模拟器验证”“真机验证”“可复现构建”“比赛交付”分成五个独立状态。

### 3. 评测集仍偏小，且召回/IoU 与 mask 质量没有完全统一

16 个目标的对象级门禁是有价值的，但不能代表开放场景准确率；部分实验还存在完整标注不足、候选匹配规则和单图隔离运行的限制。框 IoU 通过也不能证明像素级 mask 边界通过。

改进：建立固定版本的真实收纳数据集，完整标注每张图的类别、框和 mask，再单独报告 recall、precision、框 IoU、mask IoU、Dice、Boundary F-score、延迟和温升。

### 4. 模型资源过多，但主链路职责不够清晰

工程中同时出现 YOLO11、YOLOE、YOLO World、DeepLab 和 MobileCLIP 资源。部分模型导出后类别已固定，部分模型的能力没有进入主链路；资源越多，Core ML 编译和包体风险越大。

改进：明确每个模型的用途、输入输出、是否主链路、是否测试覆盖；暂时不用的模型不要进入正式提交包，或改为独立实验 target。

### 5. 视觉复刻和 AI 识别有时同时推进，优先级容易漂移

参考视频对照指出首页产品叙事、页面状态和识别边界都需要提升。后续工作在视觉、AR、模型、社区和个人页之间并行推进，容易出现“局部很强但主流程仍不够顺”的情况。

改进：比赛展示优先保证一条 90 秒内可复现主线：真实照片/相机 -> 确认 -> 方案 -> 执行 -> 完成对比。其他模块作为稳定的次级入口。

## 当前不能声称的事情

- 不能声称识别准确率是 100%；当前 100% 只属于有限对象级评测清单的召回/匹配结果。
- 不能声称已达到参考视频的像素级贴边 mask。
- 不能声称 EdgeSAM、MobileSAM 或其他未完成下载/验证的 SAM 已接入。
- 不能用模拟器截图证明真机 ARKit 世界跟踪、RoomPlan、连续移动稳定性、延迟、发热或空间锚定。
- 不能声称最新提交包在当前机器已经构建通过；2026-08-21 的实际构建失败在 Core ML 模型编译阶段。
- 不能声称云端 AI 可直接使用；它需要用户提供兼容 Responses API 的 endpoint、模型和 API Key。
- 不能把 `DemoData`、参考视频抽帧或旧 `SmartPawVideo*` 资源当成正式 App 的真实用户数据。

## 下一步优先级

### P0：先恢复可复现构建

1. 在最新提交包候选中列出 Xcode 工程实际引用的全部 `.mlpackage`。
2. 逐个用 `coremlc compile` 定位 5 个模型的具体错误，而不是只看 `xcodebuild` 汇总。
3. 确认这些模型是否真的被当前 Swift 代码使用；未使用模型从正式 target 移出。
4. 清理 DerivedData 后重新执行构建和测试，并保存完整日志与 `.xcresult`。
5. 确认 CoreSimulator 服务恢复后，再跑 iPhone 16 iOS 18.5 模拟器验证。

### P1：锁定一条比赛演示主线

固定使用独立真实照片 `external-messy-desk-note.png` 或一张现场真实照片，完成：

`采集 -> 识别确认 -> 生成本地方案 -> 选择区域 -> 执行步骤 -> 完成照 -> 前后对比`

每一步保存运行截图，确保不依赖 API Key、旧持久化数据或视频专用资源。

### P1：补真机证据

在支持设备上验证相机、ARKit、RoomPlan、连续移动 30 秒的 mask 稳定性、延迟、发热和权限拒绝路径。模拟器证据和真机证据要分开存放。

### P2：减少模型和统计歧义

建立 `model-manifest.md` 或等价表，记录模型文件、用途、输入、输出、类别词表、许可证、包体和测试覆盖。建立一份唯一的当前基线，废弃或明确标记所有历史数字。

### P2：再做精度提升

只有在主链路稳定、评测集固定后，再评估收纳领域微调模型或二阶段分割模型。优先考虑许可证、包体、移动端速度和真实边界收益，不能只追求截图上的 mask 观感。

## 推荐的后续工作记录格式

每次开发完成后，在本文件或单独日志中追加：

```text
日期：
源码目录/版本：
修改目标：
涉及文件：
真实输入素材：
验证命令：
设备与系统：
结果：
截图或 xcresult：
已知限制：
是否影响提交包：
```

## 交接入口

首次阅读顺序建议：

1. 本文件。
2. `docs/灵爪收纳-功能完成与验收状态.md`，了解功能边界。
3. `docs/对象级识别基线-2026-08-04.md`，了解当前最可靠的量化识别证据。
4. `docs/灵爪收纳-参考视频严格视觉对照.md`，了解视觉差距。
5. `docs/灵爪收纳-继续开发方向.md`，只参考其中未过期的历史思路。
6. `Services/`、`Features/Capture/`、`Features/Execution/` 和 `SmartPawTests/SmartPawTests.swift`，进入实现和验证。

最终判断：灵爪收纳已经有可展示的产品闭环和相当完整的原型工程，但下一阶段的核心不是继续增加页面，而是把最新提交包恢复到可复现构建状态，并用固定真实数据完成模拟器与真机的分层验收。
